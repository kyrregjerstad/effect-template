# Agent instructions

## Effect

- Put reusable Effect code in `packages/core` or other workspace packages.
- Use `@effect/vitest` for Effect tests.
- Prefer `it.effect` for tests of Effect programs.

## Source examples

- `.examples/` is gitignored and contains cloned source repositories for agent reference. This should be the canonical reference for library code examples.
